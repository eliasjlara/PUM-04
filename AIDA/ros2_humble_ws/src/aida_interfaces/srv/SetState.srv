# Request message
string desired_state      # Desired state to transition to ("active" or "idle")
---
# Response message
bool success              # Whether the state change was successful
string message            # Optional message describing the outcome (e.g., error message)